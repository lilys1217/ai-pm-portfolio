Pre-register: fixes EX-015 routing; EX-002 (now passing) must stay passing; nothing else moves; ±1-record noise floor applies.

Priority is decided by impact, routing by next action, both directly from the facts — correlation between them is a lint check, never a law.

boundary and probe cases improve or stabilize (EX-002-class contradictions, EX-013's symptom-vs-cause line, flaky EX-019 — the cases where actual deliberation should matter); clean cases hold; and — the honest column — TPOT-time and output cost rise, because analysis tokens are output tokens: a 60–80-token analysis roughly doubles your per-record output, so half a cent per exception drifts toward a full cent. 

Field order is computation order. Verdict-first schemas buy one-shot guesses with post-hoc essays; reasoning-first schemas buy deliberation with tokens. I found chain-of-thought inside my own contract — and my harness gets to decide if it's worth the price.